import styles from './header.scss';

import React from 'react';

const Header = () => {
    return (
        <header>
            <div className={styles.header}>Tinder 4 Movies</div>
        </header>
    );
};

export default Header;